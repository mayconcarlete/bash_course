#!/bin/bash

# Write a shell script that displays, "This script will exit with a 0 status."
# Be sure that the script does indeed exist with a 0 exit status.

echo "This script will exit with a 0 status."
exit 0